<views:Popup
  xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
  xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
  xmlns:views="clr-namespace:CommunityToolkit.Maui.Views;assembly=CommunityToolkit.Maui"
  xmlns:uranium="http://schemas.enisn-projects.io/dotnet/maui/uraniumui"
  xmlns:material="http://schemas.enisn-projects.io/dotnet/maui/uraniumui/material"
  xmlns:validation="clr-namespace:InputKit.Shared.Validations;assembly=InputKit.Maui"
  xmlns:models="clr-namespace:CharacomMaui.Presentation.Models"
  x:Class="CharacomMaui.Presentation.Dialogs.ConfirmDeleteDialog"
  x:Name="Root"
  Padding="0"
  Margin="0"
  BackgroundColor="{AppThemeBinding Light={DynamicResource Background}, Dark={DynamicResource BackgroundDark}}">

  <Border StrokeThickness="0"
          BackgroundColor="{AppThemeBinding Light={DynamicResource Background}, Dark={DynamicResource BackgroundDark}}"
          Padding="0">

    <!-- 全体レイアウト -->
    <Grid RowDefinitions="Auto, Auto, Auto, Auto, Auto"
          WidthRequest="600">
      <!-- ダイアログタイトル -->
      <Label x:Name="TitleLabel"
             Text="{Binding Source={x:Reference Root}, Path=Title}"
             FontSize="20"
             FontAttributes="Bold"
             Padding="15"
             BackgroundColor="{AppThemeBinding Light={DynamicResource ErrorContainer}, Dark={DynamicResource ErrorContainerDark}}"
             TextColor="{AppThemeBinding Light={DynamicResource OnErrorContainer}, Dark={DynamicResource OnErrorContainerDark}}"
             HorizontalTextAlignment="Center"
             HorizontalOptions="Fill"
             Grid.Row="0"/>
      <!-- プロジェクト情報 -->
      <Label Grid.Row="1"
             Text="{Binding Source={x:Reference Root}, Path=Message}"
             BackgroundColor="{AppThemeBinding Light={DynamicResource Background}, Dark={DynamicResource BackgroundDark}}"
             Padding="10"
             Margin="10"/>
      <Label Grid.Row="2"
             Text="本当に削除しますか？"
             BackgroundColor="{AppThemeBinding Light={DynamicResource Background}, Dark={DynamicResource BackgroundDark}}"
             Padding="10"
             Margin="10"/>
      <!-- 区切り線 -->
      <BoxView Grid.Row="3"
               HeightRequest="1"
               BackgroundColor="{AppThemeBinding Light={DynamicResource Outline}, Dark={DynamicResource OutlineDark}}"/>

      <!-- ボタン -->
      <Grid Grid.Row="4"
            ColumnDefinitions="*,*"
            BackgroundColor="{AppThemeBinding Light={DynamicResource Background}, Dark={DynamicResource BackgroundDark}}"
            Padding="10"
            ColumnSpacing="10">
        <Button Text="&#xE872; OK"
                FontFamily="{x:Static models:Icons.FontFamily}"
                BackgroundColor="{AppThemeBinding Light={StaticResource Error}, Dark={StaticResource ErrorDark}}"
                TextColor="{AppThemeBinding Light={StaticResource OnError}, Dark={StaticResource OnErrorDark}}"
                Grid.Column="0"
                HorizontalOptions="End"
                Clicked="OnOkClicked"/>

        <Button Text="キャンセル"
                BackgroundColor="Transparent"
                TextColor="{AppThemeBinding Light={StaticResource Outline}, Dark={StaticResource OutlineDark}}"
                BorderColor="{AppThemeBinding Light={StaticResource Outline}, Dark={StaticResource OutlineDark}}"
                BorderWidth="2"
                HorizontalOptions="Start"
                Grid.Column="1"
                Clicked="OnCancelClicked"/>
      </Grid>
    </Grid>
  </Border>
</views:Popup>
