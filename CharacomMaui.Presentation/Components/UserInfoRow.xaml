<?xml version="1.0" encoding="utf-8" ?>
<ContentView
  x:Class="CharacomMaui.Presentation.Components.UserInfoRow"
  xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
  xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
  xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
  xmlns:models="clr-namespace:CharacomMaui.Presentation.Models"
  xmlns:conv="clr-namespace:CharacomMaui.Presentation.Converters"
  x:Name="This">


  <!-- ✅ Resources -->
  <ContentView.Resources>
    <ResourceDictionary>

      <conv:OddConverter x:Key="OddConverter"/>

      <Style x:Key="UserRowBackgroundStyle"
             TargetType="Grid">

        <Setter Property="VisualStateManager.VisualStateGroups">
          <VisualStateGroupList>

            <VisualStateGroup x:Name="RowStates">

              <!-- 偶数行（非選択） -->
              <VisualState x:Name="NormalEven">
                <VisualState.Setters>
                  <Setter Property="BackgroundColor"
                          Value="{AppThemeBinding Light={StaticResource Surface}, Dark={StaticResource SurfaceDark}}"/>
                </VisualState.Setters>
              </VisualState>

              <!-- 奇数行（非選択） -->
              <VisualState x:Name="NormalOdd">
                <VisualState.Setters>
                  <Setter Property="BackgroundColor"
                          Value="{AppThemeBinding Light={StaticResource SurfaceAlt}, Dark={StaticResource SurfaceAltDark}}"/>
                </VisualState.Setters>
              </VisualState>

              <!-- 選択 -->
              <VisualState x:Name="Selected">
                <VisualState.Setters>
                  <Setter Property="BackgroundColor"
                          Value="{AppThemeBinding Light={StaticResource Secondary}, Dark={StaticResource SecondaryDark}}"/>
                </VisualState.Setters>
              </VisualState>

            </VisualStateGroup>

          </VisualStateGroupList>
        </Setter>
      </Style>


    </ResourceDictionary>
  </ContentView.Resources>

  <!-- ✅ GestureRecognizers はこれだけ -->
  <ContentView.GestureRecognizers>
    <TapGestureRecognizer Tapped="OnCardTapped"/>
  </ContentView.GestureRecognizers>

  <Grid RowDefinitions="*, Auto"
        MinimumHeightRequest="60"
        IsEnabled="True">

    <Grid x:Name="BackgroundBorder"
          Style="{StaticResource UserRowBackgroundStyle}"
          Margin="20,5,20,5"
          Padding="5"
          Grid.Row="0"
          IsEnabled="True"
          ColumnSpacing="10">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="1*"/>
        <ColumnDefinition Width="1*"/>
        <ColumnDefinition Width="1*"/>
      </Grid.ColumnDefinitions>
      <toolkit:AvatarView
        Grid.Column="0"
        WidthRequest="40"
        HeightRequest="40"
        BorderWidth="0"
        Padding="0"
        ImageSource="{Binding UserAvatarUrl, Source={x:Reference This}}"/>
      <Label Grid.Column="1"
             HorizontalTextAlignment="Start"
             x:Name="UserNameLabel"
             VerticalTextAlignment="Center"
             Text="{Binding UserName, Source={x:Reference This}}"/>

      <Label Grid.Column="2"
             HorizontalTextAlignment="Start"
             x:Name="UserEmailLabel"
             VerticalTextAlignment="Center"
             Text="{Binding UserEmail, Source={x:Reference This}}"/>

      <Label Grid.Column="3"
             HorizontalTextAlignment="End"
             x:Name="UserRoleLabel"
             VerticalTextAlignment="Center"
             Text="{Binding UserRole, Source={x:Reference This}}"/>
    </Grid>

    <!-- 下線 -->
    <BoxView Grid.Row="1"
             Margin="20,0,20,0"
             HeightRequest="1"
             BackgroundColor="{AppThemeBinding Light={StaticResource OnSurface}, Dark={StaticResource OnSurfaceDark}}"
             HorizontalOptions="Fill"/>
  </Grid>

</ContentView>